# 2차검수 오류 분석 웹앱 기획서

## 1. 목적

무신사와 29cm의 판매전환 상품을 2차검수하면서 발견한 오류를, 팀과 타부서가 실시간으로 함께 볼 수 있는 공유 웹 페이지로 만든다. 슬랙 수정요청 채널의 메시지를 자동으로 집계해 1차검수자별 오류량, 자주 틀리는 항목, 기간별 추이를 여러 각도로 보여준다.

Cowork의 개인 대시보드와 달리, 이 웹앱은 자체 슬랙 봇 토큰으로 데이터를 읽어오기 때문에 링크만 있으면 누구나 실시간으로 열어볼 수 있다.

## 2. 데이터 소스

슬랙 채널: `#카탈로그n세일운영-수정요청` (채널 ID `C02CT82KYP8`)

메시지 형식 (2차검수자가 작성):
- 1차검수자를 @멘션
- 수정이 필요한 UID(상품번호)를 나열 (줄바꿈 또는 콤마 구분, 한 줄에 여러 개 가능)
- ` / 수정사유` 형태로 사유를 붙임
- 한 메시지에 서로 다른 사유 블록이 여러 개 올 수 있음

예시:
```
@이용준 6949313
6949283
6949280 / 카테고리 변경 부탁드립니다.
```

## 3. 집계 규칙 (검증 완료)

이 규칙은 실제 채널 데이터로 검증한 것으로, 백엔드 코드(`server.js`)에 그대로 구현되어 있다.

1. 1차검수자: 메시지에서 @멘션된 사람. 표시 이름은 `이름/운영지원/...`에서 첫 `/` 앞부분, 괄호 제거.
2. UID: 5자리 이상 숫자. 콤마와 줄바꿈 모두 처리.
3. 플랫폼 판별: UID가 4로 시작하면 29cm, 그 외(5,6 등)는 무신사. (작성자가 조소희=무신사, 성예은=29cm 이지만, 담당자 변경 가능성이 있어 UID 접두어로 판별한다.)
4. 오류 항목 분류: 사유 문구의 키워드로 자동 분류 (카테고리 변경, 성별 수정, 제조연월/제조국, 소재/혼용률, 정보제공고시, 친환경 문구, 사이즈/실측, 판매옵션 불일치, 이미지, 상품명 홍보성/브랜드명 삭제, 개인정보 등). 키워드 규칙은 `server.js`의 `CATS` 배열에 정의. 매칭 안 되면 `기타`.
5. 오류 수량: UID 개수 기준(상품 수). 한 사유 블록의 UID 각각을 1건으로 집계.
6. 필터: 멘션이 있고 UID가 1개 이상인 메시지만 오류 리포트로 인정 (봇 안내, 잡담 등 제외).

## 4. 화면 구성 (다각도 뷰)

1. 기간 선택: 오늘 / 이번 주 / 사용자 지정 (시작~종료 일자)
2. 요약 카드: 총 오류 상품 수, 메시지 수, 플랫폼별(무신사/29cm) 비중
3. 1차검수자별 오류량: 막대 그래프 + 정렬 표
4. 오류 항목별 빈도: 막대 그래프 (자주 틀리는 항목 상위)
5. 일자별 추이: 라인 차트 (기간 내 날짜별 오류 상품 수)
6. 검수자 x 항목 교차: 특정 검수자가 어떤 항목을 자주 틀리는지 (히트맵 또는 표)
7. 원본 드릴다운: 특정 검수자/항목 클릭 시 해당 UID와 사유 목록

Claude Code에서 3~7번을 우선 완성하면 된다. 4,5번은 스캐폴드에 이미 기본 구현되어 있다.

## 5. 기술 구성

- 백엔드: Node.js + Express. 슬랙 Web API(`conversations.history`)를 봇 토큰으로 호출, 페이지네이션으로 기간 내 전체 메시지 수집 후 파싱·집계해 JSON 반환.
- 프론트: 정적 HTML + Chart.js. 백엔드 `/api/errors` 호출해 시각화.
- 캐시: 동일 기간 반복 조회를 줄이려면 서버 메모리 캐시(분 단위) 추가 권장.

## 6. 준비물 (Claude Code에서 설정)

1. 슬랙 봇 토큰: 사내 슬랙에 봇 앱을 만들고 아래 권한(OAuth Scope)을 부여한 뒤 채널에 봇을 초대한다.
   - `channels:history`, `channels:read` (공개 채널 기준. 비공개면 `groups:history`, `groups:read`)
   - `users:read` (멘션 이름 보강용, 선택)
   - 발급된 `xoxb-...` 토큰을 `.env`의 `SLACK_BOT_TOKEN`에 넣는다.
2. 채널 ID: `.env`의 `CHANNEL_ID` (기본 `C02CT82KYP8`).

## 7. 호스팅 선택지 (미정 상태 정리)

실시간 공유가 목적이므로 아래 중 사내 보안정책에 맞는 것을 고른다.

1. 사내 서버/내부 배포 환경: 가장 안전. 사내망에서만 접근. 개발팀 협조가 있으면 이 방식 권장.
2. 외부 PaaS(Vercel, Render 등): 배포가 가장 쉬움. 단, 슬랙 데이터가 외부로 나가므로 사내 보안정책 확인 필수. 접근제한(아래 이메일 허용목록, 사내 SSO, IP 화이트리스트, 비밀번호)을 반드시 건다.
3. 로컬 실행 후 사내 공유: 특정 PC에서 실행하고 사내망 IP로 접근. 임시/시범용으로 적합.

토큰과 슬랙 원문이 외부에 노출되지 않도록, 어떤 방식이든 접근 제한과 토큰 보관(환경변수, 시크릿 매니저)에 주의한다.

### 접근 제어: 권한 부여한 이메일만 열람 (권장)

호스팅 방식과 별개로, "내가 지정한 이메일 주소를 가진 사람만" 로그인해서 볼 수 있게 막는 것을 권장한다. 이게 링크가 유출돼도 아무나 못 보게 하는 가장 확실한 방법이다.

작동 방식:
- 페이지 진입 시 회사 계정(Google Workspace) 또는 슬랙 OAuth로 로그인시키고, 로그인한 사용자의 이메일을 확인한다.
- 그 이메일이 허용목록(allowlist)에 있으면 통과, 없으면 차단한다. 허용목록은 환경변수나 파일로 관리한다. 예: `ALLOWED_EMAILS=geonyong.park@musinsa.com,sohee.jo@musinsa.com`
- 사람을 추가하거나 뺄 때는 이 목록만 수정하면 된다(코드 수정 불필요).
- 팀 전체에 열려면 도메인 단위 허용(`@musinsa.com` 전체)도 가능하고, 특정 인원만 원하면 개별 이메일로 좁힌다.
- 참고: Vercel 등 일부 PaaS는 자체 SSO/비밀번호 보호 기능을 제공하므로 그걸 그대로 써도 된다.

구현 메모: 현재 스캐폴드의 `ACCESS_PASSWORD`(단순 비밀번호)는 임시용이다. 이메일 허용목록(OAuth 로그인 + `ALLOWED_EMAILS` 확인)은 Claude Code에서 붙이며, 이 방식이 이 요구에 가장 잘 맞는다.

### 접근 제어 구현: 구글 로그인 (확정 방향)

제공자는 구글 로그인으로 한다. 회사가 Google Workspace를 쓰면 별도 가입 없이 회사 이메일로 바로 로그인되고, 구현이 가장 단순하다. Claude Code에서 아래대로 붙이면 된다.

준비 (구글 클라우드 콘솔):
1. https://console.cloud.google.com 에서 프로젝트 생성 → "OAuth 동의 화면" 구성(내부용 User Type = Internal 로 하면 회사 계정만 로그인 가능).
2. "사용자 인증 정보" → OAuth 2.0 클라이언트 ID(웹 애플리케이션) 생성.
3. 승인된 리디렉션 URI에 배포 주소 기준 콜백을 등록. 예: 로컬 `http://localhost:3000/auth/google/callback`, 배포 시 `https://<도메인>/auth/google/callback`.
4. 발급된 클라이언트 ID/시크릿을 `.env` 에 넣는다: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`.

로그인 흐름:
1. 로그인 안 된 사용자가 페이지나 `/api/*` 에 접근하면 `/auth/google` 로 보내 구글 로그인을 시킨다.
2. 콜백에서 사용자의 이메일을 받아 소문자로 정규화한다.
3. 그 이메일이 허용목록을 통과하면(개별 이메일이 `ALLOWED_EMAILS` 에 있거나, 도메인 허용 `ALLOWED_DOMAINS` 예: `musinsa.com` 에 해당) 세션 쿠키를 발급하고 통과시킨다.
4. 통과 못 하면 "열람 권한이 없습니다. 관리자에게 요청하세요" 안내 페이지를 보여준다.
5. 로그아웃 경로 `/logout` 제공.

권장 라이브러리: `openid-client` 또는 `passport` + `passport-google-oauth20`, 세션은 `express-session`. 구현 난도는 낮다.

권한 관리: 사람 추가/삭제는 `.env` 의 `ALLOWED_EMAILS`(개별) 또는 `ALLOWED_DOMAINS`(도메인 전체)만 수정하면 된다. 코드 변경 불필요. 처음에는 팀원 몇 명만 개별 이메일로 시작하고, 필요하면 `musinsa.com` 도메인 전체 허용으로 넓히면 된다.

보안 메모: `GOOGLE_CLIENT_SECRET` 와 `SESSION_SECRET`(세션 서명용 임의 문자열)은 `.env`/시크릿 매니저에만 두고 저장소에 올리지 않는다. 배포 시 HTTPS 필수, 세션 쿠키는 `secure`, `httpOnly`, `sameSite=lax` 로 설정한다.

## 8. 실행 방법

`README.md` 참고. 요약: `.env` 작성 → `npm install` → `npm start` → 브라우저에서 `http://localhost:3000`.

## 9. Claude Code에게 넘길 때 요청 예시

"이 프로젝트를 완성해줘. server.js의 슬랙 집계 로직은 검증된 것이니 유지하고, public/index.html에 기획서 4번의 6번(검수자x항목 교차)과 7번(원본 드릴다운) 뷰를 추가해줘. 그리고 사내 배포를 위해 [고른 호스팅]에 맞는 배포 설정을 만들어줘."
